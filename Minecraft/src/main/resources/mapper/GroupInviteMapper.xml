<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.GroupInviteMapper">
    
    <select id="selectByInviteeId" resultType="com.minecraft.entity.GroupInvite">
        SELECT * FROM group_invite WHERE invitee_id = #{inviteeId} AND status = 0 ORDER BY created_at DESC
    </select>
    
    <select id="selectByGroupId" resultType="com.minecraft.entity.GroupInvite">
        SELECT * FROM group_invite WHERE group_id = #{groupId} ORDER BY created_at DESC
    </select>
    
    <select id="selectByGroupAndUser" resultType="com.minecraft.entity.GroupInvite">
        SELECT * FROM group_invite WHERE group_id = #{groupId} AND invitee_id = #{userId} AND status = 0
    </select>
    
</mapper>