<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.AITripRecommendationMapper">
    
    <select id="selectByUserId" resultType="com.minecraft.entity.AITripRecommendation">
        SELECT * FROM ai_trip_recommendation WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>
    
    <select id="selectByGroupId" resultType="com.minecraft.entity.AITripRecommendation">
        SELECT * FROM ai_trip_recommendation WHERE group_id = #{groupId} ORDER BY created_at DESC
    </select>
    
</mapper>