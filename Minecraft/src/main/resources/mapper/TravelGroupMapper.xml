<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.TravelGroupMapper">
    
    <select id="selectByUserId" resultType="com.minecraft.entity.TravelGroup">
        SELECT tg.* FROM travel_group tg 
        INNER JOIN group_member gm ON tg.id = gm.group_id 
        WHERE gm.user_id = #{userId} AND tg.deleted = 0
    </select>
    
    <select id="searchPublicGroups" resultType="com.minecraft.entity.TravelGroup">
        SELECT * FROM travel_group 
        WHERE is_public = 1 AND deleted = 0 
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR destination LIKE CONCAT('%', #{keyword}, '%'))
    </select>
    
</mapper>