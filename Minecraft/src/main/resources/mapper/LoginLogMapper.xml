<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.LoginLogMapper">
    <select id="selectLoginLogPage" resultType="com.minecraft.entity.LoginLog">
        SELECT * FROM login_log
        <where>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%') OR ipaddr LIKE CONCAT('%', #{keyword}, '%') OR account LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="startTime != null and startTime != ''">
                AND login_time >= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND login_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY login_time DESC
    </select>
</mapper>