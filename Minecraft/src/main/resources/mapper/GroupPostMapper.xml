<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.GroupPostMapper">
    
    <select id="selectByGroupId" resultType="com.minecraft.entity.GroupPost">
        SELECT * FROM group_post WHERE group_id = #{groupId} AND deleted = 0 ORDER BY created_at DESC
    </select>
    
    <update id="incrementLikeCount">
        UPDATE group_post SET like_count = like_count + 1 WHERE id = #{postId}
    </update>
    
    <update id="decrementLikeCount">
        UPDATE group_post SET like_count = like_count - 1 WHERE id = #{postId} AND like_count > 0
    </update>
    
    <update id="incrementCommentCount">
        UPDATE group_post SET comment_count = comment_count + 1 WHERE id = #{postId}
    </update>
    
</mapper>