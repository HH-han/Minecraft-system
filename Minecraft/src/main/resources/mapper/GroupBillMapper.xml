<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.GroupBillMapper">
    
    <select id="selectByGroupId" resultType="com.minecraft.entity.GroupBill">
        SELECT * FROM group_bill WHERE group_id = #{groupId} ORDER BY bill_date DESC
    </select>
    
    <select id="selectUnsettledByGroupId" resultType="com.minecraft.entity.GroupBill">
        SELECT * FROM group_bill WHERE group_id = #{groupId} AND is_settled = 0 ORDER BY bill_date DESC
    </select>
    
</mapper>