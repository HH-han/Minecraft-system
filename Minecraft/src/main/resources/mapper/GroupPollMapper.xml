<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.GroupPollMapper">
    
    <select id="selectByGroupId" resultType="com.minecraft.entity.GroupPoll">
        SELECT * FROM group_poll WHERE group_id = #{groupId} ORDER BY created_at DESC
    </select>
    
    <select id="selectActiveByGroupId" resultType="com.minecraft.entity.GroupPoll">
        SELECT * FROM group_poll WHERE group_id = #{groupId} AND status = 1 ORDER BY created_at DESC
    </select>
    
</mapper>