<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.EmojiMapper">
    <select id="selectByCategory" resultType="com.minecraft.entity.Emoji">
        SELECT * FROM emoji 
        WHERE category = #{category} 
          AND is_active = 1
        ORDER BY sort_order ASC
    </select>
    
    <select id="selectAllActive" resultType="com.minecraft.entity.Emoji">
        SELECT * FROM emoji 
        WHERE is_active = 1
        ORDER BY category ASC, sort_order ASC
    </select>
    
    <select id="selectAllCategories" resultType="java.lang.String">
        SELECT DISTINCT category FROM emoji WHERE is_active = 1 ORDER BY category ASC
    </select>
</mapper>