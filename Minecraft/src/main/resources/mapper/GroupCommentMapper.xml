<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minecraft.mapper.GroupCommentMapper">
    
    <select id="selectByPostId" resultType="com.minecraft.entity.GroupComment">
        SELECT * FROM group_comment WHERE post_id = #{postId} AND deleted = 0 ORDER BY created_at ASC
    </select>
    
    <select id="selectByParentId" resultType="com.minecraft.entity.GroupComment">
        SELECT * FROM group_comment WHERE parent_id = #{parentId} AND deleted = 0 ORDER BY created_at ASC
    </select>
    
</mapper>